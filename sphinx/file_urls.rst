
File Urls
*********

File URLs represent resources that are acessible on the local file system, so the
:py:meth:`FileUrl.get_resource()` just returns ``self``

.. automodule:: appurl.file
   :members:

